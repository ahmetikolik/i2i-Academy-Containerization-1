version: '3.8'

services:
  web_server:
    image: nginx:latest
    container_name: i2i-web-container
    ports:
      - "8080:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
    restart: always